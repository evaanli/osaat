// Draw the object itself
draw_self();

/* // Draw a red circle showing the maximum reach of your collision_line
draw_set_color(c_red);
draw_circle(x, y, 60, true); // Change '60' to whatever your _dist is currently set to
draw_set_color(c_white);

// Draw the Left "Found" line
draw_set_color(c_green);
draw_line(x, y, x + lengthdir_x(100, left_angle_debug), y + lengthdir_y(100, left_angle_debug));

// Draw the Right "Found" line
draw_set_color(c_red);
draw_line(x, y, x + lengthdir_x(100, right_angle_debug), y + lengthdir_y(100, right_angle_debug));

// Draw the actual direction
draw_set_color(c_yellow);
draw_line(x, y, x + lengthdir_x(120, direction), y + lengthdir_y(120, direction));

draw_set_color(c_white); */